/**
 * Shared storefront shell — header, body sections, footer.
 * Modest responsive side gutters (tighter on mobile, a bit more on wide screens).
 */
export const STOREFRONT_CONTAINER =
  'mx-auto w-full max-w-[90rem] px-4 sm:px-6 md:px-8 lg:px-10 xl:px-12';

/** Admin panel — same max-width and responsive gutters as the storefront. */
export const ADMIN_CONTAINER = STOREFRONT_CONTAINER;
